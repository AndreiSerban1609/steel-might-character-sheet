package com.steelmight.charactersheet.engine;

/** Damage category per damage-types.json: physical (reduced by PA), magical (MA), true (nothing). */
public enum DamageCategory {
    PHYSICAL,
    MAGICAL,
    TRUE
}
